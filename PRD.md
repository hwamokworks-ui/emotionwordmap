**감정 어휘 지도 — PRD (제품 요구사항 문서)**
버전 v1.0 · 작성 기준: stage1~6 워크시트 + 워킹 스켈레톤 화면(index/find/map/archive) 구현 결과

> 이 문서는 1~6단계 워크시트 내용을 그대로 옮긴 요약본이 아니라, 그 결과물을 근거로 "왜 이렇게 만들었는가"를 설명하는 문서다. 각 섹션 끝에 붙은 근거는 해당 결정이 어느 워크시트의 어떤 데이터·인터뷰·검증에서 나왔는지를 명시한다.

---

## ① 프로젝트 개요

**서비스명**: 감정 어휘 지도
**한 줄 정의**: 상황이나 막연한 느낌에서 출발해, 감정 간의 위치·관계를 지도 위에서 탐색하며 자신에게 가장 가까운 감정 어휘를 발견하고 표현하도록 돕는 웹 서비스.

**현재 개발 단계**: 로그인·서버 DB 없이 브라우저 `localStorage`에 상태를 저장하는 **워킹 스켈레톤** 단계. `index.html`(감정 찾기) → `map.html`(감정 지도) → `archive.html`(감정 서고) 3개 핵심 화면이 루트에 나란히 있는 형태로 실제 동작하는 프로토타입이며, 모바일·터치 환경까지 반응형으로 대응한다. 이 PRD는 이 프로토타입을 정식 제품으로 전환하기 위한 요구사항을 정의한다.

**왜 이 아이디어인가**: 1단계에서 발산한 9개 앱 아이디어(노후 플래너, 날씨 감정 케어, 멘탈케어 연결 등) 중 "감정 어휘 확장 및 이모티콘 추천 앱"을 최종 선택했다. 근거는 두 가지다.
- **주관적 근거**: 자신의 감정을 정확한 언어로 표현하기 어려운 문제는 AI가 개인화된 어휘·표현을 추천하는 방식으로 확장 가능성이 뚜렷했다.
- **정량적 근거(데이터랩 검증)**: `감정어휘` 키워드가 비교 키워드 5개 중 평균 검색량 8.4로 1위, 10개 연령대 중 다수에서 1위를 차지해 특정 세대에 한정되지 않는 잠재 수요가 확인됐다. `감정기록`은 평균 6.1로 2위이며 25~34세에 검색이 집중돼, 초기 타깃을 좁힐 근거가 됐다.

이후 2~5단계를 거치며 아이디어의 방향(단순 이모티콘 추천 → 감정 간 관계를 탐색하는 지도형 경험)이 구체화됐고, 그 결과가 지금의 화면 구조다. 즉 이 프로젝트는 "새 아이디어를 기획"하는 것이 아니라 "이미 검증하며 만든 워킹 스켈레톤을 개발 착수 가능한 스펙으로 정리"하는 단계에 있다.

---

## ② 문제 정의

**문제 정의 (한 줄)**: 자신의 감정을 느끼면서도 적절한 감정 단어를 찾지 못해, 자신의 마음을 정확하고 다양하게 표현하기 어렵다.

**근본 원인 (5 Whys 결론)**: 자신의 감정을 구체적으로 인식하고 표현할 수 있는 감정 어휘와, 이를 자신의 경험과 연결해 탐색할 기회가 부족하기 때문이다.

**왜 "어휘 부족"이 아니라 "탐색 기회 부족"으로 문제를 좁혔는가**: 5 Whys를 표면적인 답("감정 단어를 모른다")에서 멈추지 않고 계속 파고든 결과, "단어를 많이 보여주는 것만으로는 충분하지 않다 → 가까운 감정·먼 감정·비슷하지만 다른 감정을 비교할 수 있어야 자신의 경험과 연결된다"는 결론에 도달했다. 이 지점이 경쟁 서비스(How We Feel, Daylio 등, ③ 참고)와의 차별점을 만드는 지점이기도 하다. 단순 사전식 감정 단어 검색 앱이 아니라 "관계를 탐색하는 지도"로 서비스를 정의한 이유가 여기서 나온다.

**HMW (기회로 전환)**
| HMW | 내용 | 이 PRD에서의 취급 |
|---|---|---|
| HMW1 | 감정 어휘를 탐색·확장해 자신의 감정을 더 정확히 이해·표현하게 한다 | **핵심 축.** ④~⑤의 Must 기능 전체가 이 HMW를 해결하도록 설계됨 |
| HMW2 | 관계 속에서 감정을 표현해야 하는 사람이 적절한 단어를 찾고 전달하게 한다 | **의도적으로 후순위.** 최종 페르소나가 "관계 전달형(김민지·박서연)"이 아닌 "자기이해형(이준호)"으로 좁혀지면서 Should 등급(표현 문장·이모티콘)까지만 반영하고, 표현 대상 선택 같은 완전한 해결은 다음 버전 과제로 남김 |
| HMW3 | 일상에서 감정이 생긴 순간마다 비슷한/다른 감정을 탐색하며 자연스럽게 발견하게 한다 | 감정 지도의 근접 감정·반의어·조금 다른 감정 탐색 기능으로 반영 |

---

## ③ 타겟 사용자

**최종 페르소나**: 이준호 (29세, IT 기획자)
- **목표**: 지금 느끼는 감정을 정확히 알고, 나에게 맞는 단어로 표현하고 싶다.
- **페인포인트**: 감정이 복잡하거나 여러 감정이 섞였을 때 어떤 단어로 표현해야 할지 모호하다. 단순 좋음/나쁨 기록만으로는 자신의 감정을 충분히 이해하기 어렵다.
- **행동 습관**: 감정이 생기면 바로 표현하기보다 혼자 생각하며 정리한다. 자기이해·기록·자기관리 서비스를 적극적으로 탐색하고 시도한다.
- **기술 친숙도**: 매우 높음 — 새로운 서비스 시도에 거부감이 없다.

**이 페르소나로 좁힌 근거**
1. **실제 인터뷰**: "일기도 결국 누군가 볼 수 있기 때문에 솔직하기 어렵다"는 답변에서 감정을 안전하게 혼자 기록할 환경의 필요성을 확인했다. 반대로 "감정 표현에 어려움이 없고 굳이 필요를 느끼지 못한다"는 답변을 통해, 감정 표현이 이미 편한 사용자는 핵심 타깃에서 제외했다.
2. **AI 가상 페르소나 3인 비교**: 김민지(관계 속 표현형)·박서연(가족 갈등형)·이준호(자기이해형) 중 "혼자 생각하고 정리하는" 이준호형을 채택했다. 나머지 두 유형의 니즈(HMW2)는 완전히 버리지 않고 Should 등급 기능(표현 문장, 이모티콘 추천)으로 일부만 반영했다.
3. **데이터랩 트렌드**: `감정어휘`는 전 연령대에서 고르게 검색되지만, `감정기록`은 25~29세(13.60)·30~34세(15.35)에서 뚜렷하게 높아 20대 후반~30대 초반 자기이해형 사용자와 일치한다.

**의도적으로 초기 타깃에서 제외한 사용자**: 관계 속 감정 전달이 주 목적인 사용자(HMW2 대상). 소셜 공유·전문가 상담 연결 같은 기능도 MoSCoW에서 Won't로 분류했는데, 이는 기능 부족이 아니라 "혼자 안전하게 탐색하는 경험"이라는 핵심 가치와 상충하기 때문이다(④ 참고).

---

## ④ 핵심 기능 명세 (Must 기준)

4단계 MoSCoW에서 Must로 분류된 기능을 실제 사용자 흐름 순서로 재배열했다. 괄호 안은 왜 이 기능이 Must인지의 근거다.

**1) 탐색 시작 (index.html)**
| 기능 | 설명 | Must로 지정한 근거 |
|---|---|---|
| 감정 입력 | 자유 문장으로 지금 마음을 적으면 관련 감정으로 연결 | 페르소나 목표("정확히 알고 표현하고 싶다")의 기본 진입점 |
| 감정 검색 | 이미 알고 있는 단어로 바로 탐색 시작 | 어휘를 이미 아는 사용자의 마찰 제거 |
| 상황으로 감정 찾기 | "애인과 헤어졌을 때" 등 상황 카드에서 시작 | "여러 감정이 섞이면 단어를 모르겠다"는 페인포인트를 상황이라는 우회 경로로 직접 해결 |
| 감정 단어 예시·추천 | 오늘의 한 단어, 요즘 많이 찾는 감정 | 저니맵 ②첫 사용 단계의 "무엇을 입력할지 막막함"을 완충 |

**2) 감정 지도 핵심 탐색 (map.html)**
| 기능 | 설명 | Must로 지정한 근거 |
|---|---|---|
| 감정 지도 | 10개 영역·434개 단어를 노드-엣지 그래프로 표시, 팬/줌 지원(모바일은 터치) | 서비스의 핵심 차별 기능(경쟁사 대비 유일하게 "위치·관계"로 감정을 보여줌) |
| 감정 영역 탐색 | 영역 단위로 관련 어휘를 넓게 탐색 | HMW1·3(어휘 확장·자연스러운 발견)과 직결 |
| 유사·관련·반대 감정 탐색 | 가까운 감정(그래프 이웃) / 반의어 / 조금 다른 감정 chip | "비슷한 감정의 미묘한 차이"를 판단하는 핵심 비교 도구 |
| 감정 간 거리·관계 확인 | 근접 감정에 붙는 거리(%) | 감정을 하나의 정답이 아니라 위치로 이해하게 하는 근본원인 해결책 |
| 감정 비교·제외 | 근접/반의어 칩을 눌러 다른 후보로 이동 | "정답을 바로 고르지 않고 발견하는 과정" 자체가 가치라는 컨셉을 구현 |
| 감정 상세 정보 | 정의·품사·강도·캐릭터 장면 | 최종 후보가 실제 자신의 경험인지 판단하는 단계 |
| 감정이 나타나는 상황(예문) | "이렇게 표현할 수 있어요" 예문 | 사전적 정의만으로는 부족하다는 페인포인트를 실제 상황 문장으로 보완 |
| 감정 기록 | 마음 기록 textarea + 저장 | 감정 탐색을 일회성으로 끝내지 않게 하는 핵심 장치 |
| 개인 기록 관리 | 기록은 본인만 조회(향후 계정 단위 비공개) | 인터뷰의 "일기도 누군가 볼 수 있어 솔직하기 어렵다"를 직접 해결하는, 가장 인터뷰 근거가 뚜렷한 Must 기능 |

**3) 축적 (archive.html)**
| 기능 | 설명 | Must로 지정한 근거 |
|---|---|---|
| 기록 히스토리 | 발견/저장/마음기록을 일·월·연도별로 조회 | 과거-현재 감정을 연결해 돌아보려는 재방문 동기 |

Should 등급(표현 문장·이모티콘·감정 도감·리포트·뱃지)은 워킹 스켈레톤에 부분 반영됐고, Won't 등급(소셜 공유, 전문가 상담 연결, AI 자동 진단)은 "혼자 안전하게 스스로 발견한다"는 핵심 가치와 충돌해 지금 범위에서 명시적으로 제외한다.

---

## ⑤ 화면 구성 및 사용자 흐름

**왜 14단계 스켈레톤이 4개 화면으로 압축됐는가**: 5단계에서 설계한 워킹 스켈레톤은 감정 탐색 시작 → 후보 탐색 → 지도 미리보기 → 영역 탐색 → 관계 탐색 → 비교 → 상세 → 표현 → 기록 → 히스토리 → 시각화 → 리포트 → 도감 → 뱃지까지 14단계였다. 실제 구현에서는 이를 정보 구조 관점에서 3개 화면으로 통합했다.
- 2~8단계(후보 탐색~표현)는 지도 위 상세 패널 하나로 합쳐졌다. 팝업이 아니라 **우측 고정 패널**로 만들어, 지도를 계속 보면서 탐색과 상세 확인을 동시에 할 수 있게 했다(탐색 자체가 핵심 경험이므로 화면 전환으로 흐름을 끊지 않기 위함).
- 9~14단계(기록~뱃지)는 "감정 서고" 한 화면에 탭과 그래프로 압축했다. 6개 화면으로 쪼개면 재방문했을 때 어디를 봐야 할지 산만해진다는 판단에서다.

**화면 흐름**
```
index.html (감정 찾기 — 루트 진입점)
  ├─ 자유 입력 / 상황 카드 / 오늘의 단어 / 인기 감정 → 단어 매칭
  ↓
map.html (감정 지도, ?w=<word_id>로 진입)
  ├─ 지도 팬/줌 탐색(모바일은 터치 팬/핀치줌), 노드 클릭 시 발견 처리
  └─ 우측 상세 패널: 정의·강도·근접감정·반의어·예문·마음기록·저장 (좁은 화면에서는 화면 대부분을 덮는 시트로 전환)
  ↓ (상단 네비게이션으로 언제든 이동)
archive.html (감정 서고)
  └─ 발견/저장/기록 통계, 기간별 그래프, 지역 완성도, 뱃지
```
**파일 구조 변경**: 원래는 `screens/find.html`을 `index.html`이 리다이렉트하는 2단 구조였고, 별도로 흐름 전체를 소개하는 정적 랜딩 화면(`screens/index.html`)도 있었다. 그런데 이 랜딩 화면이 아무 데서도 링크되지 않는 고아 페이지였고, 사용자가 별도의 랜딩 페이지를 새로 만들 계획이라 삭제했다. 동시에 감정 찾기 화면 자체를 루트 `index.html`로 승격시켜 리다이렉트 단계를 없앴다 — 지금은 `screens/` 폴더 없이 `index.html`·`map.html`·`archive.html` 세 파일이 루트에 나란히 있다.

**5단계 페르소나 재검증에서 남은 갭 (다음 버전 과제로 명시)**
| 갭 | 왜 지금은 보류했는가 |
|---|---|
| 심리적 안전 안내("나만 볼 수 있어요")가 index.html(감정 찾기)에만 있고 지도 상세패널·감정 서고에는 없음 | 워킹 스켈레톤 단계에서는 핵심 탐색 흐름 구현을 우선했다. 인터뷰 근거가 가장 뚜렷한 갭이므로 ⑦ 비기능 요구사항에 필수 보완 항목으로 승격했다 |
| 감정 리포트의 서술형 해석("당신은 최근 ○○을 자주 느꼈어요")이 없고 그래프만 존재 | 집계 그래프까지가 Should 최소 구현 범위였고, 해석 텍스트 생성은 별도 로직(또는 AI 요약)이 필요해 범위를 다음 버전으로 넘김 |
| 표현 대상 선택·이모티콘 추천 미구현 | HMW2(관계 속 전달) 자체가 이번 페르소나 범위 밖이라 우선순위에서 밀림 |

---

## ⑥ 데이터 구조

6단계에서 화면을 역분석해 도출한 데이터 항목을 Supabase(PostgreSQL) 테이블로 정리했다. 왜 이 구조인지는 화면 요소에서 왔다 — 화면에 존재하지 않는 컬럼은 만들지 않았다.

| 테이블 | 성격 | 대표 컬럼 |
|---|---|---|
| `users` | 계정 (Auth 연동) | display_name, avatar_initial, status |
| `emotion_regions` | 콘텐츠 마스터 — 관리자가 채움 | name, root_word_id, color, mood, **cx/cy/rx/ry** |
| `emotion_words` | 콘텐츠 마스터 (434개) | noun_form, region_id, intensity, definition, example_sentence, **display_order** |
| `emotion_word_relations` | 단어 간 인접 관계 (620개 엣지) | word_a_id, word_b_id |
| `intensity_labels` | 콘텐츠 마스터 — 강도 라벨 문구 | intensity(PK), label |
| `hero_greetings` | 콘텐츠 마스터 — 시간대별 인사말 | from_hour, to_hour, text |
| `user_word_discoveries` | 유저 행동 — 발견 기록 | user_id, word_id, discovered_at |
| `user_word_saves` | 유저 행동 — 저장 기록 | user_id, word_id, saved_at |
| `user_notes` | 유저 행동 — 마음 기록 | user_id, word_id, content, **situation**, created_at |
| `badges` / `user_badges` | 성취 조건 마스터 / 달성 기록 | condition_type, threshold / earned_at |
| `situation_prompts` | 상황 카드 콘텐츠 | label, linked_word_id |
| `search_keywords` | 자연어 검색 매핑 | keyword, word_id |
| `daily_words` / `weekly_trending_words` | 집계·큐레이션 콘텐츠 | date+word_id / week+rank+discoverer_count |

**테이블 관계의 핵심 구조**: `users`와 `emotion_words`가 두 개의 허브이고, 그 사이를 `user_word_discoveries`·`user_word_saves`·`user_notes`·`user_badges`가 "누가 무엇을 했는지"로 잇는다. `emotion_regions`·`emotion_words`·`emotion_word_relations`는 관리자가 미리 채우는 콘텐츠이고 나머지는 사용자 행동으로 쌓인다. 이렇게 콘텐츠와 행동 데이터를 분리한 이유는, 감정 단어·관계망은 서비스 운영 중 거의 바뀌지 않는 반면 사용자 행동 데이터는 계속 늘어나기 때문이다 — 둘을 섞으면 이후 콘텐츠를 업데이트할 때 사용자 데이터를 건드릴 위험이 커진다.

**설계 검증에서 발견해 반영 완료한 항목**: 최초 설계에는 화면에 실제로 표시되는데도 빠져 있던 4가지가 있었다. 지금은 위 표에 모두 통합했다 — ① 지도 위 10개 영역의 위치·크기(`emotion_regions.cx/cy/rx/ry`, 없으면 영역이 어디에 얼마나 크게 그려질지 DB만으로 결정할 수 없음) ② 단어가 영역 안에서 배치되는 순서(`emotion_words.display_order` — 이 값과 영역 좌표를 "골든 앵글 스파이럴" 공식에 넣어 겹치지 않는 위치를 계산한다. 공식과 재구현 방법은 `design-system/DESIGN.md` §3.1.1 참고. 순서가 없으면 저장 순서가 보장되지 않아 지도 모양이 로드마다 흔들릴 수 있음) ③ 강도 옆 문구("매우 약함~매우 강함")를 위한 `intensity_labels` 테이블(코드에 고정돼 있으면 문구 변경마다 배포가 필요함) ④ index.html(감정 찾기) 시간대별 인사말 6종을 위한 `hero_greetings` 테이블(마찬가지로 카피 변경에 배포가 필요한 문제 해결).

**stage5 워킹 스켈레톤 대조에서 추가로 발견한 항목**: stage5의 14단계 흐름(실제 구현 범위보다 넓음)까지 대조한 결과 6개 항목이 더 빠져 있었다. 이 중 `user_notes.situation`(text) — "감정+상황+메모"를 하나로 기록하고, 이후 "상황별 감정 패턴" 시각화에 쓰기 위한 컬럼 — 만 이번에 스키마에 반영했다. 나머지 5개(감정 후보 제외 기록용 `user_word_dismissals`, 표현 대상용 `expression_targets`, 대상·상황별 표현 문장용 `expression_sentences`, 이모티콘 추천용 컬럼, 감정 리포트 요약 저장용 `emotion_reports`)는 아직 해당 화면 자체가 구현 전이므로 스키마에 넣지 않고 "발견된 과제"로만 기록해둔다. 해당 기능(표현 대상, 이모티콘, 감정 리포트, 감정 제외)을 실제로 만들기로 확정하는 시점에 그때 가서 추가한다.

**감정 어휘 확장 방법론과 10번째 영역**: 원래 145개 단어는 전부 수작업으로 만들어진 가짜 목업이었고, 이후 KOTE(43개 감정 분류)·EVOKE(검증된 한국어 감정 형용사 425개)·HuggingFace(valence/arousal 실측 데이터, CC0) 3종을 대조해 10번째 영역("혐오의 늪")과 경계 단어 3개(경멸하다·우쭐하다·비장하다)를 추가하며 161개까지 늘렸다. 이 단계까지도 여전히 뜻풀이·예문 등은 목업이었다.

**목업 161개 전량 폐기 → HuggingFace 실제 데이터 434개로 전면 교체(최신)**: KOTE·EVOKE는 CC BY-NC(비영리) 라이선스라 단어·정의를 그대로 복사할 수 없고, EVOKE는 애초에 valence/arousal 같은 연속 수치를 수집하지 않아(이진 판정 방식) 지도 배치에 쓸 "거리" 계산이 불가능하다. 반면 HuggingFace 데이터(CC0, 434개 고유 단어)는 representative·prototypicality·familiarity·valence·arousal을 전부 수치로 제공해 실제 최종 어휘 선정에 썼다. 선정·분류 기준의 상세 방법론은 `docs/감정지도_어휘기준.md`에 정리했고, PRD 관점에서 중요한 결정만 요약하면:
- **1차 204개 + 2차 230개, 2단계로 채워 434개 완성** — 1차는 representative=true 또는 prototypicality·familiarity가 중앙값 이상인 품질 필터 통과분.
- **통계 분류(최근접 중심점)만으로는 부족해 434개 전부를 사람이 재검토** — "분노의 영역"과 "혐오의 늪"의 valence·arousal 중심점이 지나치게 가까워 "화나다"·"분노하다" 같은 명백한 분노 단어까지 통계상 혐오로 잘못 분류되는 문제가 반복돼, 434개 중 약 절반을 재배정했다.
- **관계 엣지 620개 신규 생성** — 영역별 valence·arousal 거리 기반 최소 신장 트리 + 최근접 이웃 2개로 계산, 옛 223개(삭제된 단어 참조)는 폐기.
- **영역 크기 재조정** — 단어 수가 영역마다 14~70개로 크게 벌어져 `rx`/`ry`를 영역별 단어 수 기준(중앙값 대비 제곱근 배율)으로 다시 계산하고 좌표 격자를 재배치했다. 알고리즘 상세는 `design-system/DESIGN.md` §3.1.1 참고.

**localStorage → Supabase 전환이 필요한 이유**: 지금은 브라우저 하나에 모든 상태(발견·저장·기록)가 묶여 있어 기기를 바꾸면 기록이 사라진다. 페르소나가 "혼자 꾸준히 자기 감정을 돌아보는" 사용자인 만큼, 기기 간 동기화와 계정 기반 비공개 저장(RLS)이 정식 서비스의 최소 요건이다.

---

## ⑦ 비기능 요구사항

**심리적 안전 / 프라이버시 (최우선)**
- 근거: 실제 인터뷰 "일기도 결국 누군가 볼 수 있기 때문에 솔직하기 어렵다"가 이 서비스의 가장 직접적인 반대 근거이자 채택 근거였다.
- 요구사항: `user_notes`·`user_word_discoveries`·`user_word_saves`는 Supabase RLS로 본인 계정만 조회/수정 가능해야 한다. "이 지도는 나만 볼 수 있어요" 안내는 index.html(감정 찾기)뿐 아니라 감정 지도 상세패널과 감정 서고에도 동일하게 노출한다(⑤에서 지적된 갭 보완). 소셜 공유·타인 열람 기능은 만들지 않는다(MoSCoW Won't).

**플랫폼**
- 웹 우선. 페르소나의 기술 친숙도가 매우 높고 "인터랙티브 지도(팬/줌/노드 클릭)" 정도의 복잡도에 거부감이 없다는 5단계 검증 결과를 근거로, 네이티브 앱 대신 웹 인터랙션 난이도를 그대로 유지한다.
- **모바일 반응형 (구현 완료)**: 처음엔 "데스크톱 기준 반응형"이라고만 적어두고 실제로는 데스크톱 레이아웃만 있었다. 이후 상단 내비게이션(좁은 화면에서 축소·가로 스와이프), 감정 찾기의 2단 레이아웃(모바일에서 1단으로 스택), 지도 상세 패널(760px 이하에서 전체 화면에 가까운 시트로 전환)을 실제로 손봤다. 특히 지도는 원래 마우스 드래그·휠로만 팬/줌이 가능해 터치스크린에서는 조작 자체가 안 됐는데, 한 손가락 드래그(팬)·두 손가락 핀치(줌)를 추가해 폰·태블릿에서도 핵심 탐색 경험이 동작하게 됐다.

**성능**
- 지도 화면은 434개 노드 + 620개 엣지를 SVG로 동시에 렌더링하고 팬/줌 인터랙션을 실시간 처리해야 한다. 단어·영역 수가 늘어날 가능성을 감안해 렌더링은 뷰포트 기반으로 최적화한다.

**디자인 원칙 (경쟁사 대비 의도적 회피)**
- Finch류의 캐릭터 육성·꾸미기 게이미피케이션은 배제한다(MoSCoW Could, 채택 안 함) — 페르소나가 "쾌활한 게임 경험"보다 "사색적인 자기이해"를 원하기 때문이다.
- How We Feel류의 임상적 폼 형태, Daylio류의 즉석 이모지 체크 UI도 피하고, 대신 감정을 위치·관계로 탐색하는 지도 경험을 유지한다.

**보안**
- 인증은 Supabase Auth 기반으로 하고, 콘텐츠 마스터 테이블(`emotion_words`, `emotion_regions` 등)은 관리자만 쓰기 가능, 일반 사용자는 읽기 전용으로 제한한다.

**운영/확장성**
- 감정 단어·영역·관계·상황 카드·오늘의 단어·인기 감정은 모두 콘텐츠 데이터이므로, 개발자 배포 없이 값을 수정할 수 있는 관리 수단(Supabase 테이블 직접 편집 또는 간단한 어드민 화면)이 필요하다.

---

## ⑧ 성공 지표

1단계에서 "가설로 남겨뒀던" 질문들을 워킹 스켈레톤 단계에서 관찰 가능한 지표로 전환했다. 지금은 결제 여부를 검증할 단계가 아니므로, 유료 전환 이전 단계의 **참여·재방문 지표**를 우선한다.

| 검증하려는 가설 (stage1~2 원문) | 성공 지표 | 지표 선정 근거 |
|---|---|---|
| "감정 어휘 탐색이 지속적인 사용을 유도할 수 있는가?" | 감정 서고 주간 재방문율, 1인당 주간 평균 발견 단어 수 | 일회성 이탈이 아니라 반복 탐색이 일어나는지가 핵심 원가치이므로 |
| "감정 간 거리·관계 기반 추천이 실제 감정과 공감을 얻는가?" | 상세패널에서 "가까운 감정/반의어/조금 다른 감정" 칩 클릭률, 클릭 후 저장 전환율 | 근접도 기반 추천이 실제로 탐색을 이어가게 만드는지를 직접 보여주는 행동 지표 |
| "감정 어휘 확장이 감정 이해에 실제로 도움이 되는가?" | 마음 기록 작성률(발견 대비 기록 비율), 기록 텍스트 재방문 열람률 | 단순 발견 수가 아니라 "기록까지 이어졌는가"가 이해로 연결됐다는 대리 지표 |
| "캐릭터·뱃지 등 게임 요소가 지속 사용을 유도하는가?" | 뱃지 달성률, 뱃지 달성 전후 7일 재방문율 변화 | Should 등급으로 넣은 게이미피케이션 요소가 실제 효과가 있는지 검증 |
| "감정 단어 추천에 비용을 지불할 의향이 있는가?" (아직 미검증) | 지금 단계 KPI에서는 제외 — 위 참여 지표가 일정 수준 이상 확인된 뒤, 심화 리포트·감정 도감 등 Should 기능을 유료 문 뒤에 배치했을 때의 전환율로 다음 단계에서 측정 | 데이터랩 검색 관심도는 지불 의사의 증거가 아니라고 1단계에서 이미 명시했으므로, 실제 결제 데이터 없이 성공 지표로 못 박지 않는다 |

이 문서는 stage1~6의 결정과 워킹 스켈레톤 구현을 근거로 작성되었으며, ⑤·⑥에서 명시한 갭(안내 문구 확장, 리포트 해석 텍스트, 지도 좌표·정렬·강도 라벨 테이블화)은 다음 개발 스프린트의 선행 작업으로 이관한다.
